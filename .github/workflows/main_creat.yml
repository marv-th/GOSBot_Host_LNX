name: CI2

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        echo Yeah
    - name: Create GOSZip
      uses: juankaram/archive-action@v1.0.0
      with:
        args: "zip -r gosout.zip instance"
