name: Create Archive
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: marv-th/GOSBot_Host_LNX@master
    - name: Archive Release
      run: |
        zip -R gosbot_build.zip *
